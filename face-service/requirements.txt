# SmartEducation Face Service dependencies
#
# Install with:  pip install -r requirements.txt
#
# NOTE: For the PRIMARY engine (app_deepface.py - DeepFace/Facenet) you need flask,
# deepface, tensorflow, cryptography, python-dotenv (all listed below).
#
# The ALTERNATE engine (app.py - face_recognition/dlib) additionally requires:
#   face-recognition>=1.3.0   (requires cmake + dlib - see INSTALL_WINDOWS.md)

flask>=3.0.0
flask-cors>=4.0.0
deepface>=0.0.90
tensorflow-cpu>=2.15,<2.22
tf-keras>=2.15
opencv-python-headless>=4.5,<5
numpy>=1.24.0
pillow>=10.0.0
python-dotenv>=1.0.0
cryptography>=41.0.0